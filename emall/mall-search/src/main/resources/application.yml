server:
  port: 9201

elastic:
  host: #
  port: 9200

spring:
  application:
    name: mall-search

  hystrix:
    command:
      default:
        execution:
          isolation:
            thread:
              timeoutInMilliseconds: 6000

  cloud:
    loadbalancer:
      retry:
        enabled: true

ribbon:
  ReadTimeout: 60000
  ConnectTimeout : 60000


