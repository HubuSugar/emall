<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="edu.hubu.mall.dao.SkuSaleAttrValueDao">
    <select id="getSkuItemSaleAttrValuesBySpuId" resultType="edu.hubu.mall.vo.AttrVo">
        SELECT
            b.attr_id,
            b.attr_name,
            GROUP_CONCAT(DISTINCT b.attr_value) AS attr_value
        FROM
            pms_sku_info a
            LEFT JOIN pms_sku_sale_attr_value b ON a.sku_id = b.sku_id
        WHERE
            spu_id = #{spuId}
        GROUP BY
            b.attr_id,
            b.attr_name
    </select>
</mapper>
